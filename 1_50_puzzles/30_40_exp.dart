import 'dart:io';

import 'dart:math';

void main() {
  //                                {Example 31}

  //                                {Example 32}

  //                                {Example 33}

  //                                {Example 34}
  // int a;
  // int b;
  // int? grade;
  // a = int.parse(stdin.readLineSync()!);
  // b = int.parse(stdin.readLineSync()!);
  //
  // grade = pow(2, a) as int?;
  // print(pow(2, grade!) % b);
  //                                {Example 35}

  // int count = 0;
  // int l;
  // int r;
  // bool deter;
  // deter = true;
  // l = int.parse(stdin.readLineSync()!);
  // r = int.parse(stdin.readLineSync()!);
  // while(deter){
  //   if()
  // }

  //                                {Example 36}
  //                                {Example 37}

  // int limit;
  // limit = int.parse(stdin.readLineSync()!);
  // int count = 1;
  // int summa = 0;
  // int a;
  // int b;
  //
  // for (int i = 1; i <= limit; i++) {
  //   a = int.parse(stdin.readLineSync()!);
  //   b = int.parse(stdin.readLineSync()!);
  //   if (limit >= 2) {
  //     summa = (b - a) + 1;
  //     count *= summa;
  //     print(count);
  //   } else {
  //     summa = (b - a) + 1;
  //     print(summa);
  //   }
  // }

  //                                {Example 38}

  //                                {Example 39}

  // int phone;
  // phone = int.parse(stdin.readLineSync()!);
  //
  // for (int i = 1; i <= phone; i++) {
  //   if ((phone - i) % 100 == i) {
  //     print(phone - i);
  //   }
  // }

  //                                {Example 40}

  // int ovchilar;
  // int qoldiq;
  // int qolganBaliq = 1;
  // ovchilar = int.parse(stdin.readLineSync()!);
  // qoldiq = int.parse(stdin.readLineSync()!);
  // qolganBaliq = ovchilar;
  // for (int i = qolganBaliq; i <= 60; i + ovchilar) {
  //   for (int k = 1; k <= ovchilar; k++) {
  //
  //   }
  // }
}
